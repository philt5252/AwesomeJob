<UserControl x:Class="LdarDataDisplay.Core.Views.DeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="HeaderBrush">
            <GradientStop Color="#E55B5B5B"></GradientStop>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="RowBrush">
            <GradientStop Color="#4C5B5B5B"></GradientStop>
        </LinearGradientBrush>
    </UserControl.Resources>
    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="4,4,4,4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="{StaticResource HeaderBrush}">
                <Label  HorizontalAlignment="Center" VerticalAlignment="Center" 
               Content="Device 1" FontSize="32" FontWeight="DemiBold"  ></Label>
            </Grid>
            <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Stretch" >
                <StackPanel.Resources>
                    <Style TargetType="{x:Type Label}">
                        <Setter Property="Margin" Value="0,15,0,0" />
                        <Setter Property="FontSize" Value="20"></Setter>
                        <Setter Property="FontWeight" Value="DemiBold"></Setter>
                    </Style>
                </StackPanel.Resources>
                <StackPanel Orientation="Horizontal" Background="{StaticResource RowBrush}" >
                    <Label Width="230" Content="Time Since Last Update: "></Label>
                    <Label Content="{Binding DateTime}"></Label>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Width="230" Content="LPH2: "></Label>
                    <Label Content="{Binding LPH2}"></Label>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Background="{StaticResource RowBrush}">
                    <Label Width="230" Content="Detector Temp: " ></Label>
                    <Label Content="{Binding DetectorTemp}"></Label>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Width="230" Content="Voltage: " ></Label>
                    <Label Content="{Binding Voltage}"></Label>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Background="{StaticResource RowBrush}">
                    <Label Width="230" Content="H2 Pressure: "></Label>
                    <Label Content="{Binding H2Pressure}"></Label>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Width="230" Content="PPM: " ></Label>
                    <Label Content="{Binding PPM}"></Label>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Background="{StaticResource RowBrush}">
                    <Label Width="230" Content="Pump PPL: " ></Label>
                    <Label Content="{Binding PumpPPL}"></Label>
                </StackPanel>
            </StackPanel>

        </Grid>
    </Border>
</UserControl>
